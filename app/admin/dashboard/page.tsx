import DashboardPage from './dashboard-client';

export const metadata = {
  title: 'Dashboard | Shipy',
  description: 'Overview of fleet metrics and active vessels.',
};

export default function Page() {
  return <DashboardPage />;
}
