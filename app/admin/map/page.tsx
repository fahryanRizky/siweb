import AdminMapPage from './map-client';

export const metadata = {
  title: 'Active Fleet Map | Shipy',
  description: 'Real-time positioning map of active vessels.',
};

export default function Page() {
  return <AdminMapPage />;
}
