import SignupPage from './signup-client';

export const metadata = {
  title: 'Sign Up | Shipy',
  description: 'Create a new account on Shipy.',
};

export default function Page() {
  return <SignupPage />;
}
